{
  "name": "localflow-desktop",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev:ui": "npm --prefix ui run dev",
    "build:ui": "npm --prefix ui run build",
    "dev:tauri": "npm --prefix ui run dev",
    "tauri:dev": "cd src-tauri && cargo tauri dev"
  }
}
